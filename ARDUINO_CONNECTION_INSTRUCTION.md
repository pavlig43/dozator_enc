# Куда подключать провода к плате с ATmega328P

Перед подключением выключите питание.

Номера D и A ниже совпадают с нумерацией Arduino Nano. На плате ставится сам ATmega328P, модуль Arduino не нужен. Та же схема задана в `include/pin_destination.h`.

## LCD 16×2

```text
GND → GND
VCC → 5V
SDA → A4 / SDA
SCL → A5 / SCL
```

## Нажимной энкодер

```text
S1  → D7
S2  → D6
KEY → D5
+   → 5V
−   → GND
```

Код включает внутреннюю подтяжку входов. Короткое нажатие выбирает действие, удержание от 1 с открывает меню. Отдельной кнопки MENU нет. Если вес при повороте меняется не в ту сторону, поменяйте S1 и S2 местами.

## Весовой модуль HX711

```text
VCC → 5V
GND → GND
DT / DOUT → D9
SCK / CLK → D10
```

Тензодатчик подключается к `E+`, `E−`, `A+`, `A−` модуля HX711.

## Двигатель шнека и DRV8871

Управляющий разъём драйвера:

```text
IN1 → D2
IN2 → D3
GND → общая земля
```

Двигатель и питание 12 В подключите к силовым выводам DRV8871. Логика и драйвер должны иметь общую землю.

## RGB-светодиод

```text
B → резистор 220 Ом → D13
G → резистор 220 Ом → D12
R → резистор 220 Ом → D11
общий вывод → GND
```

## Таблица пинов

| Узел | Логический пин |
|---|---|
| DRV8871 IN1 | D2 |
| DRV8871 IN2, ШИМ | D3 |
| Энкодер KEY | D5 |
| Энкодер S2 | D6 |
| Энкодер S1 | D7 |
| HX711 DT | D9 |
| HX711 SCK | D10 |
| RGB R | D11 |
| RGB G | D12 |
| RGB B | D13 |
| LCD SDA | A4 |
| LCD SCL | A5 |
