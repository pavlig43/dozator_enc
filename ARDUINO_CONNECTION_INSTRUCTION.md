# Куда подключать провода

Перед подключением выключите питание.

## LCD 16×2

```text
GND → GND
VCC → 5V
SDA → A4 / SDA
SCL → A5 / SCL
```

## Нажимной энкодер

```text
S1  → D8
S2  → D9
KEY → D10
+   → 5V
−   → GND
```

Код включает внутреннюю подтяжку входов. Если вес при повороте меняется не в ту сторону, включите `setEncReverse(true)` или поменяйте S1 и S2 местами.

## Кнопка MENU без фиксации

```text
Первый вывод → D4
Второй вывод → GND
```

Внешний резистор не нужен: D4 работает как `INPUT_PULLUP`.

## Весовой модуль HX711

```text
VCC → 5V
GND → GND
DT / DOUT → D3
SCK / CLK → D2
```

Тензодатчик подключается к `E+`, `E−`, `A+`, `A−` модуля HX711.

## Двигатель шнека и MOSFET

Управляющий разъём MOSFET:

```text
PWM → D11
GND → GND Arduino
```

Силовой разъём:

```text
− источника → минус блока двигателя
+ источника / + мотора → плюс блока и плюс двигателя
− мотора → минус двигателя
```

Питание двигателя остаётся 12 В. Земля MOSFET и Arduino должна быть общей.

## RGB-светодиод

```text
B → резистор 220 Ом → D5
G → резистор 220 Ом → D6
R → резистор 220 Ом → D7
общий вывод → GND
```

## Таблица пинов

| Узел | Пин Arduino |
|---|---|
| HX711 SCK | D2 |
| HX711 DT | D3 |
| Кнопка MENU | D4 |
| RGB B | D5 |
| RGB G | D6 |
| RGB R | D7 |
| Энкодер S1 | D8 |
| Энкодер S2 | D9 |
| Энкодер KEY | D10 |
| MOSFET шнека | D11 |
| LCD SDA | A4 |
| LCD SCL | A5 |
