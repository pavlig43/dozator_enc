# Энкодер и кнопка MENU

## Главное меню

После пуска LCD показывает выбранный экран:

```text
Select screen
> Work
```

| Действие | Результат |
|---|---|
| Поворот энкодера влево | Предыдущий пункт |
| Поворот энкодера вправо | Следующий пункт |
| Нажатие энкодера | Открыть пункт |
| Кнопка `MENU` | В меню ничего не делает |

Пункты идут по кругу:

```text
Weight input -> Feed setup -> Calibration -> Work
```

## Целевой вес

```text
Target weight, g
950
```

- Поворот вправо добавляет 50 г.
- Поворот влево вычитает 50 г.
- Диапазон: 50–99950 г.
- Кнопка `MENU` сохраняет вес и открывает меню.

## Порог досыпки

```text
Stop weight, g
50
```

- Поворот вправо добавляет 1 г.
- Поворот влево вычитает 1 г.
- Диапазон: 1–999 г.
- Кнопка `MENU` сохраняет порог и открывает меню.

## Калибровка весов

На пустых весах нажмите энкодер:

```text
Press encoder
to zero
```

После обнуления поставьте груз 1 кг и ждите:

```text
Put 1kg
wait...
```

После записи коэффициента нажмите `MENU`:

```text
OK saved
MENU to exit
```

## Работа

Когда шнек не работает, LCD показывает `PUSH`:

```text
PUSH 0
Target 950
```

- Нажатие энкодера останавливает шнек, обнуляет весы и запускает новый цикл.
- Кнопка `MENU` сразу останавливает шнек и открывает меню.
- Поворот энкодера на рабочем экране ничего не меняет.
